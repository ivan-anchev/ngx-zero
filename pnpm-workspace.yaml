# Dependency build scripts are deliberately disabled: @rocicorp/zero-sqlite3 is
# server-side only, esbuild/protobufjs work fine without their postinstall.
allowBuilds:
  '@rocicorp/zero-sqlite3': false
  esbuild: false
  protobufjs: false
