packages:
  - 'examples/*'

# esbuild/protobufjs work fine without their postinstall. zero-sqlite3 must
# build: the playground runs zero-cache, which needs the native module.
allowBuilds:
  '@rocicorp/zero-sqlite3': true
  esbuild: false
  protobufjs: false
